# === Core Dependencies ===
fastapi==0.109.0
uvicorn==0.27.0
gunicorn==21.2.0

# === Database ===
sqlalchemy==2.0.25
pyodbc==5.1.0
python-dotenv==1.0.0

# === Azure Services ===
azure-storage-blob==12.16.0  # Pinned for stability
azure-cosmos==4.5.0

# === AI/Embeddings ===
sentence-transformers==2.2.2
httpx==0.27.0  # For async HTTP (replace requests where possible)

# === Data Processing ===
pandas==2.2.1  # Pinned for SQLAlchemy compatibility
openpyxl==3.1.2  # Excel support (xlrd removed - deprecated)
python-multipart==0.0.7  # FastAPI file uploads

# === Pydantic ===
pydantic==2.5.3
pydantic-settings==2.1.0  # Add this line